spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/accessibledocs
    username: postgres
    password: 2634Hp$$
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: true
    validate-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080

ai:
  service:
    url: http://localhost:8000/simplify

jwt:
  secret: verySecretKeyForHackathonThatIsLongEnoughToMeet256BitRequirement
  expiration: 86400000

cors:
  allowed-origins: http://localhost:3000,http://localhost:3001